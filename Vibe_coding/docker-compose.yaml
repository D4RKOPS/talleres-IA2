version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./data/tasks.db
    volumes:
      - ./src:/app/src
      - sqlite_data:/app/data
    restart: unless-stopped

volumes:
  sqlite_data:
